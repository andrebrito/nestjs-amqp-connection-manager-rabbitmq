const EXCHANGE = 'testing.exchange';
const QUEUE = 'testing.queue';
const ROUTING_KEY = 'testing.routing.key';

const CURRENT_TRY = 'max.tries';
const MAX_TRIES = 3;

export { EXCHANGE, QUEUE, ROUTING_KEY, CURRENT_TRY, MAX_TRIES };
